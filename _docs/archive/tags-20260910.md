# 태그 정리 — 옮긴 것 (2026-09-10)

대표 결정으로 프리즈 태그를 정리했다(`COMMON_STANDARDS.md` §23-6).

> "못을 너무 많이 박으면 고정인가? 망침이지 — 오히려 이정표가 정신 사나워진다."

**지우지 않았다. 옮겼다.** 아래 태그들은 `refs/archive/tags/<원래이름>`으로 옮겨서
`git tag`와 GitHub 태그 목록에는 안 보이지만, **커밋은 그대로 살아 있다.**

```bash
git checkout refs/archive/tags/poster-studio-freeze-20260903   # 언제든 돌아갈 수 있다
git show refs/archive/tags/poster-studio-freeze-20260903       # 태그 메시지도 그대로
```

**옮긴 태그의 커밋은 전부 `master`의 조상이다**(옮기기 전에 7개 전부 확인함). 즉 태그가
없어도 커밋 해시로 언제든 닿는다 — 아래 표에 해시를 남기는 이유가 그것이다.

## 남긴 것 (2개) — 성격을 갈라 적는다

**복구용**(그대로 되돌리면 지금도 동작하는 지점)과 **대조용**(되돌려 쓸 수는 없지만 비교
대상이 되는 지점)은 다르다. 섞어 두면 급할 때 대조용을 복구용으로 착각한다
(`COMMON_STANDARDS.md` §23-6).

| 태그 | 커밋 | 성격 | 왜 남기는가 |
|---|---|---|---|
| `poster-studio-freeze-20260910-audited-100` | `c6608b9` | **복구용** | **지금 기준점.** 9차 종합감사 100/100 직후다. 이후 커밋 4개는 문서와 CI 잡 조건뿐이라 앱 동작은 라이브와 같다. |
| `poster-studio-freeze-20260909-pre-react-merge` | `ab2eb31` | **대조용** | **리액트 전환 직전의 화면·코드를 비교할 지점.** 되돌리기 버튼으로는 더 이상 쓸 수 없다 — 아래 참고. |

### `-pre-react-merge`가 왜 복구용이 아닌가 (2026-09-10 재확인)

머지 당일(9/9)에는 `git revert -m 1 5fb346f`가 이 트리를 **파일 차이 0건**으로 재현했다.
**그 뒤 master에 커밋 25개가 쌓이면서 지금은 그 revert가 깨끗이 적용되지 않는다** —
실제로 임시 워크트리에서 눌러 보니 **충돌 9건**이었다(`.claude/rules/app.md`,
`.github/workflows/deploy.yml`, `AGENTS.md`, `CLAUDE.md`, `index.html`, `package.json`,
`scripts/verify/compare.mjs`, `test/poster-studio.test.tsx`, `test/react-setup.ts`).

트리를 통째로 `git checkout ab2eb31` 하는 것은 여전히 된다. 다만 그렇게 하면 **9/10에
들어간 것이 전부 사라진다** — 특히 **취약점 8건을 0건으로 만든 `functions/package.json`의
`overrides`가 그 시점엔 없다.** 그 밖에 한도 강제 함수와 그 테스트, 카카오톡·SNS 공유
카드(og 태그 + `og.jpg`), 진행자 런북 재작성, 문서 커밋에 배포를 건너뛰는 CI 잡 조건도
그 시점 이후의 것이다.

**확인한 것 하나 더**: Voice Cinema가 겪은 함정(옛 태그의 CORS 허용 목록에 정본 주소가
없어 되돌리면 업로드가 막힘)은 **이 저장소에는 해당하지 않는다.** `poster.edutogether.kr`은
9/9 커밋 `46d6caa`에서 이미 `ALLOWED_ORIGINS`에 들어갔고, `ab2eb31` 시점의
`functions/index.js`에도 앵커까지 그대로 있다(직접 확인).

## 옮긴 것 (5개)

| 태그 | 커밋 | 날짜 | 메시지 첫 줄 |
|---|---|---|---|
| `poster-studio-freeze-20260826` | `3ce0db1c60ba100377d04f72145345f8fe2ddea3` | 2026-08-26 | 정밀감사 전 프리즈 지점 (20260826) |
| `poster-studio-freeze-20260829` | `a76a78fe9fc4ee8c8540e615a8f575e8a36437c8` | 2026-08-29 | 정식 종합감사 전 프리즈 지점 (20260829, inky-poster-studio 이전 후 첫 정식 감사) |
| `poster-studio-freeze-20260903` | `dcc3de7f318412b1bd3a3240de1a754d8b1d7c8e` | 2026-09-03 | 7차 종합감사 100/100 확정 + IP_RATE_LIMIT_MAX=50(3-IP 와이파이 구성) 반영 시점 프리즈 |
| `poster-studio-freeze-20260907-pre-vitest` | `d57faf456c9e8f33038f5233eae4372445a14982` | 2026-09-06 | vitest 이전 작업 직전 복구 지점 |
| `poster-studio-freeze-20260909-pre-react` | `5e43449e8eec9f5214f755c13545eadb3295dd06` | 2026-09-09 | 리액트+TS 전환 착수 직전 스냅샷 (2026-09-09) |

## 두 리액트 태그 중 왜 `-pre-react-merge`를 남겼나

둘 다 "리액트 전환 직전"처럼 보이지만 **가리키는 시점이 다르고, 전환 직전의 라이브 화면을
담은 것은 하나뿐이다.**

- `pre-react`(`5e43449`, 09-09 **00:35**) — 전환 **작업을 시작하기 전**. 이 커밋은
  `master`와 전환 브랜치의 갈라진 지점(merge-base)이기도 하다.
- `pre-react-merge`(`ab2eb31`, 09-09 **19:33**) — 전환본이 `master`에 머지되기 **직전의
  라이브 상태**.

**그 사이에 커밋 27개가 있고, 그날 대표 지시로 라이브에 나간 화면 변경이 전부 거기 있다** —
앱 이름 변경, 헤더 확대, 개인정보 링크 위치, `privacy.html` 재작성, 캐시 규칙 수정,
개인/단체 스위치, 라벨을 입력칸 옆으로, ※ 안내 영역, 초기화 버튼, 여백 재배분,
폰트 서브셋(첫 로드 3.8MB → 157KB).

즉 **`pre-react`로 되돌리면 리액트 전환만이 아니라 그날 대표가 직접 지시한 화면 작업이
통째로 사라진다.** 전환 직전 상태를 대표하지 못한다. `pre-react-merge`가 맞고, 그쪽은
**실제로 되돌려서 파일 차이 0건까지 확인**해 둔 지점이다(그 확인은 머지 당일 기준이다 —
지금은 대조용이라는 것을 위에 적어 뒀다).

## 디자인 개편 지점을 안 남긴 이유

`poster-studio-freeze-20260903`(`dcc3de7`, 09-03 **01:21**)은 공교롭게도 그날 새벽의
**디자인 4~7라운드 직전**이다(4라운드 04:22, 7라운드 06:47). "개편 전 화면"으로 돌아갈
이정표가 될 수 있어 남길지 검토했고, **안 남기기로 했다**:

- 그 뒤로 화면은 7라운드를 거쳐 수렴했고, **9/9에 대표가 직접 보면서 다시 여러 번
  고쳤다.** 지금 시점에 09-03 이전 화면으로 돌아가는 것은 그 전부를 버리는 일이라
  11/14 전에 실제로 할 만한 선택이 아니다.
- 그날 이후 **"그 전 화면을 보자"는 요청이 한 번도 없었다.**
- 커밋은 그대로 살아 있고 `_docs/CHANGELOG.md`가 라운드별로 커밋을 짚어 둔다 —
  보고 싶으면 `git checkout dcc3de7f31`이면 된다.

**필요해지면 그때 다시 태그를 세우면 된다.** 못은 필요할 때 박는 것이지 미리 박아
두는 것이 아니다.

## 옮기기 전 확인 — "이 태그에만 있는 파일"이 있는가

옮기는 태그의 파일 목록을 `master` + 남기는 태그 2개의 합집합과 대조했다. **어느 태그에도
잃을 원본은 없었다.** 옮기는 쪽에만 있는 파일은 3개이고 전부 대체된 것들이다:

| 파일 | 어디에만 | 무엇 |
|---|---|---|
| `.github/workflows/pages.yml` | `20260826`, `20260829` | GitHub Pages 배포. Firebase Hosting 이전으로 폐기 |
| `.github/workflows/functions-deploy.yml` | `20260829` | 분리돼 있던 Functions 배포. 지금 `deploy.yml` 하나로 합쳐짐 |
| `RUNBOOK.md`(저장소 루트) | `20260903`, `20260907-pre-vitest` | 지금은 `_docs/ops/runbook.md` |

**폰트 원본도 확인했다** — 서브셋(`public/fonts/subset/`, 각 40KB)을 도입한 뒤에도
**원본 woff2 6개(각 약 780KB)가 `master`에 그대로 있다.** 서브셋 전 원본이 옛 태그에만
남아 있는 상황이 아니다.

그리고 애초에 **7개 태그의 커밋이 전부 `master`의 조상**이므로, 옮긴 뒤에도 커밋 해시로
닿을 수 있다.
