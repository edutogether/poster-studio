# 태그 정리 — 옮긴 것 (2026-09-10)

대표 결정으로 프리즈 태그를 정리했다(`COMMON_STANDARDS.md` §23-6).

> "못을 너무 많이 박으면 고정인가? 망침이지 — 오히려 이정표가 정신 사나워진다."

**지우지 않았다. 옮겼다.** 아래 태그들은 `refs/archive/tags/<원래이름>`으로 옮겨서
`git tag`와 GitHub 태그 목록에는 안 보이지만, **커밋은 그대로 살아 있다.**

```bash
git checkout refs/archive/tags/poster-studio-freeze-20260903   # 언제든 돌아갈 수 있다
git show refs/archive/tags/poster-studio-freeze-20260903       # 태그 메시지도 그대로
```

**옮긴 태그의 커밋은 전부 `master`의 조상이다**(옮기기 전에 7개 전부 확인함). 즉 태그가
없어도 커밋 해시로 언제든 닿는다 — 아래 표에 해시를 남기는 이유가 그것이다.

## 남긴 것 (2개)

| 태그 | 커밋 | 왜 남기는가 |
|---|---|---|
| `poster-studio-freeze-20260910-audited-100` | `c6608b9` | **지금 기준점.** 9차 종합감사 100/100 직후이자 현재 라이브 상태다. |
| `poster-studio-freeze-20260909-pre-react-merge` | `ab2eb31` | **리액트 전환을 되돌릴 실제 지점.** `git revert -m 1 <머지커밋>`이 이 트리를 **파일 차이 0건**으로 재현하는 것을 실제로 실행해 확인했다. |

## 옮긴 것 (5개)

| 태그 | 커밋 | 날짜 | 메시지 첫 줄 |
|---|---|---|---|
| `poster-studio-freeze-20260826` | `3ce0db1c60ba100377d04f72145345f8fe2ddea3` | 2026-08-26 | 정밀감사 전 프리즈 지점 (20260826) |
| `poster-studio-freeze-20260829` | `a76a78fe9fc4ee8c8540e615a8f575e8a36437c8` | 2026-08-29 | 정식 종합감사 전 프리즈 지점 (20260829, inky-poster-studio 이전 후 첫 정식 감사) |
| `poster-studio-freeze-20260903` | `dcc3de7f318412b1bd3a3240de1a754d8b1d7c8e` | 2026-09-03 | 7차 종합감사 100/100 확정 + IP_RATE_LIMIT_MAX=50(3-IP 와이파이 구성) 반영 시점 프리즈 |
| `poster-studio-freeze-20260907-pre-vitest` | `d57faf456c9e8f33038f5233eae4372445a14982` | 2026-09-06 | vitest 이전 작업 직전 복구 지점 |
| `poster-studio-freeze-20260909-pre-react` | `5e43449e8eec9f5214f755c13545eadb3295dd06` | 2026-09-09 | 리액트+TS 전환 착수 직전 스냅샷 (2026-09-09) |

## 두 리액트 태그 중 왜 `-pre-react-merge`를 남겼나

둘 다 "리액트 전환 직전"처럼 보이지만 **가리키는 시점이 다르고, 되돌아갈 지점은 하나뿐이다.**

- `pre-react`(`5e43449`, 09-09 **00:35**) — 전환 **작업을 시작하기 전**. 이 커밋은
  `master`와 전환 브랜치의 갈라진 지점(merge-base)이기도 하다.
- `pre-react-merge`(`ab2eb31`, 09-09 **19:33**) — 전환본이 `master`에 머지되기 **직전의
  라이브 상태**.

**그 사이에 커밋 27개가 있고, 그날 대표 지시로 라이브에 나간 화면 변경이 전부 거기 있다** —
앱 이름 변경, 헤더 확대, 개인정보 링크 위치, `privacy.html` 재작성, 캐시 규칙 수정,
개인/단체 스위치, 라벨을 입력칸 옆으로, ※ 안내 영역, 초기화 버튼, 여백 재배분,
폰트 서브셋(첫 로드 3.8MB → 157KB).

즉 **`pre-react`로 되돌리면 리액트 전환만이 아니라 그날 대표가 직접 지시한 화면 작업이
통째로 사라진다.** 되돌리기 지점으로 쓸 수 없다. `pre-react-merge`가 맞고, 그쪽은
**실제로 되돌려서 파일 차이 0건까지 확인**해 둔 지점이다.

## 디자인 개편 지점을 안 남긴 이유

`poster-studio-freeze-20260903`(`dcc3de7`, 09-03 **01:21**)은 공교롭게도 그날 새벽의
**디자인 4~7라운드 직전**이다(4라운드 04:22, 7라운드 06:47). "개편 전 화면"으로 돌아갈
이정표가 될 수 있어 남길지 검토했고, **안 남기기로 했다**:

- 그 뒤로 화면은 7라운드를 거쳐 수렴했고, **9/9에 대표가 직접 보면서 다시 여러 번
  고쳤다.** 지금 시점에 09-03 이전 화면으로 돌아가는 것은 그 전부를 버리는 일이라
  11/14 전에 실제로 할 만한 선택이 아니다.
- 그날 이후 **"그 전 화면을 보자"는 요청이 한 번도 없었다.**
- 커밋은 그대로 살아 있고 `_docs/CHANGELOG.md`가 라운드별로 커밋을 짚어 둔다 —
  보고 싶으면 `git checkout dcc3de7f31`이면 된다.

**필요해지면 그때 다시 태그를 세우면 된다.** 못은 필요할 때 박는 것이지 미리 박아
두는 것이 아니다.
