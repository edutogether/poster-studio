# 로컬 에뮬레이터 테스트용. 실제 배포에서는 이 파일을 쓰지 않고
# firebase functions:secrets:set OPENAI_API_KEY 로 Firebase Secret Manager에 등록한다.
OPENAI_API_KEY=sk-...

# 운영 옵션(선택, 기본값 그대로 써도 됨)
# IMAGE_MODEL=gpt-image-2
# IMAGE_QUALITY=medium
# IMAGE_SIZE=1024x1536
# VARIANTS=1
# INPUT_FIDELITY=high
