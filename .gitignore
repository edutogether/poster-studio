node_modules/
functions/node_modules/
uploads/
.env
functions/.env
functions/.env.*
!functions/.env.example
*.log
.cache
.firebase/
.claude/settings.local.json

# 빌드 산출물. master는 아직 빌드를 쓰지 않지만, 전환 브랜치를 오가며 작업하면
# dist/가 작업 폴더에 남아 있어 master에서 `git add -A`에 딸려 들어간다(2026-09-09 실제로 겪음).
dist/
